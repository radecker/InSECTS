syntax = "proto2";

message Telemetry {
    optional string destination = 1;
    optional string sender = 2;

    message SystemStatus {
        optional bool health = 1;
    }
}

message Commands {

}